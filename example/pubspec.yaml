name: enhanced_speed_dial_example
description: Example app for the Enhanced Speed Dial package.
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  enhanced_speed_dial:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  # Additional testing dependencies
  mockito: ^5.4.4
  build_runner: ^2.4.9
  golden_toolkit: ^0.15.0

flutter:
  uses-material-design: true
